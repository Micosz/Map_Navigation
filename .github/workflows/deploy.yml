# Future Use: GitHub Actions CI/CD for AWS Lambda deployment
# We will not use auto-deploy until after the project presentation.

# name: Deploy to AWS Lambda
# on:
#   push:
#     branches:
#       - main

# jobs:
#   deploy:
#     runs-on: ubuntu-latest
#     steps:
#       - uses: actions/checkout@v3
#       - name: Set up Python
#         uses: actions/setup-python@v4
#         with:
#           python-version: '3.9'
#       - name: Zip Deployment Package
#         run: zip -j deployment.zip backend/app.py infrastructure/graph.json
#       - name: AWS Credentials
#         uses: aws-actions/configure-aws-credentials@v1
#         with:
#           aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
#           aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
#           aws-region: us-east-1
#       - name: Deploy Lambda Function
#         run: aws lambda update-function-code --function-name IndoorNavLambda --zip-file fileb://deployment.zip
